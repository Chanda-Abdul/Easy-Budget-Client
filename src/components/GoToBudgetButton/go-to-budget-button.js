import React from 'react';

export default function GoToBudgetButton(props) {
    return (
        <div className="button-demo">
            <button>Review your Budget</button>
        </div>
    );
}
